import 'package:flutter/material.dart';

class AppColors {
  // Colores principales inspirados en Vodafone
  static const Color primary = Color(0xFFE60000);       // Rojo Vodafone principal
  static const Color secondary = Color(0xFF333333);     // Gris oscuro
  static const Color surface = Color(0xFFF8F8F8);       // Gris muy claro para cards
  static const Color background = Color(0xFFFFFFFF);    // Blanco puro

  // Variaciones del rojo Vodafone
  static const Color accent = Color(0xFFE60000);        // Rojo Vodafone
  static const Color accentLight = Color(0xFFFFEBEB);   // Rojo muy suave para fondos
  static const Color accentDark = Color(0xFFB30000);    // Rojo oscuro para hover

  // Grises modernos para texto y elementos UI
  static const Color textPrimary = Color(0xFF1A1A1A);   // Negro suave
  static const Color textSecondary = Color(0xFF666666); // Gris medio
  static const Color textTertiary = Color(0xFF999999);  // Gris claro

  // Estados con colores modernos
  static const Color success = Color(0xFF00C851);       // Verde Vodafone
  static const Color warning = Color(0xFFFF8800);       // Naranja
  static const Color error = Color(0xFFE60000);         // Rojo Vodafone
  static const Color info = Color(0xFF007CBA);          // Azul corporativo

  // Elementos UI con diseño moderno
  static const Color border = Color(0xFFE0E0E0);        // Bordes suaves
  static const Color divider = Color(0xFFEEEEEE);       // Divisores
  static const Color shadow = Color(0x10000000);        // Sombras más pronunciadas

  // Hover y pressed states
  static const Color hover = Color(0xFFF5F5F5);         // Hover claro
  static const Color pressed = Color(0xFFEEEEEE);       // Pressed
}